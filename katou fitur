Keyword yang digunakan

-- Katou d

-- Katou keyword d

-- Hai Katou d

-- Katou perkenalkan dirimu d

-- Katou tendang si kelinci d

-- Katou berapa (angka) perlu diketahui untuk kali menggunakan "*",bagi menggunakan "/", dan sisa hasil bagi menggunakan "%" d

-- Katou ramal d

-- Katou apa itu (teks teks) d e

-- Katou cari gambar (teks teks) d e

-- Katou ucapkan selamat ulang tahun ke (nama) d

-- Katou cuaca (nama kota) d e

-- Katou cari video (teks teks) d e

-- Katou download musik (teks teks) d e

-- Katou stalk (id instagram) d e

-- Katou terjemahkan (kode bahasa-kode bahasa) (teks yang ingin diterjemahkan) untuk mengetahui kode bahasa yang di support buka https://github.com/Rehre/katou-megumi-LINEBot d

-- Katou cari lokasi (teks lokasi) d e

-- Katou tulis (Teks yang ingin dirubah ke gambar) d

-- Katou 9gag (Section di 9gag, Kalau dibiarkan kosong akan mengambil dari section hot) untuk melihat list section buka "Katou help 9gag" d e

-- Katou ubah alay (teks yang ingin diubah) d

-- Katou terjemahkan alay (teks yang ingin diterjemahkan) catatan : teks yang dapat diterjemahkan hanya teks alay yang didapet dari katou d

-- tambahkan exception handling

-- pindahkan callback ke node js

-- buat testing node js app di heroku

-- ganti repo katou di github

-- buat website teka-teki katou

-- tambahkan teka teki

-- tambahkan fitur osu

Jika ada masalah silahkan kontak :
Created by : Rehre(id:akl2340 18 tahun) ig: _rehre
Bot ini diciptakan untuk memenuhi kebutuhan personal

{
  "type": "template",
  "altText": "Stalk",
  "template": {
    "type": "carousel",
    "columns": [{
        "thumbnailImageUrl": "https://a.ppy.sh/" + hasil_profile[0].user_id,
        "title": hasil_profile[0].username,
        "text": deskripsi_profil,
        "actions": [{
          "type": "uri",
          "label": "Ke Profil",
          "uri": "https://osu.ppy.sh/u/" + hasil_profile[0].user_id
        }, {
          "type": "uri",
          "label": "Download Recent Beatmap",
          "uri": "https://osu.ppy.sh/d/" + hasil_recent[0].beatmap_id
        }]
      },
      {
        "thumbnailImageUrl": "https://b.ppy.sh/thumb/" + hasil_recent[0].beatmap_id + ".jpg",
        "title": "Lagu Terakhir Dimainkan",
        "text": deskripsi_best,
        "actions": [{
          "type": "uri",
          "label": "Ke Profil",
          "uri": "https://osu.ppy.sh/u/" + hasil_profile[0].user_id
        }, {
          "type": "uri",
          "label": "Download Recent Beatmap",
          "uri": "https://osu.ppy.sh/d/" + hasil_recent[0].beatmap_id
        }]
      }
    ]
  }
}
